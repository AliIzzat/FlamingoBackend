<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{title}}</title>

  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/backend.css">
</head>

<body class="backend">
    <main class="backend-main">
      <header class="backend-topbar">
        {{#if user}}
          <div style="font-size: 18px;color:crimson">Logged in as: {{user.name}} ({{user.role}})</div>
        {{/if}}
      </header>

      <section class="backend-content">
        {{{body}}}
      </section>
    </main>
  </div>
</body>
</html>
